// @ai:generated by=claude-fable-5 date=2026-08-12
package redis

import (
	goredis "github.com/redis/go-redis/v9"
)

// Driver Redis 驅動實現
// KeyDB 兼容 Redis 協議，使用者同樣用 redis.New()
//
// 與 hidb/mysql、hidb/pg 的 Dialect 同規格：子套件只提供驅動身分
// 與 client 建構，連線池參數、ping 驗證、資源追蹤 hook 一律由
// hidb core 統一處理
type Driver struct{}

// New 創建 Redis 驅動實例
func New() *Driver {
	return &Driver{}
}

// Name 返回驅動名稱（對應 config.yaml 的 database.driver）
func (d *Driver) Name() string {
	return "redis"
}

// NewClient 建立 go-redis client
func (d *Driver) NewClient(opt *goredis.Options) *goredis.Client {
	return goredis.NewClient(opt)
}
